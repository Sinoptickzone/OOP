package Classes;

/**
 * Represents an ordinary client in the system, extending the Actor class.
 */
public class OrdinaryClient extends Actor {
    /**
     * Constructor for OrdinaryClient class.
     * @param name The name of the ordinary client.
     */
    public OrdinaryClient(String name) {
        super(name);
    }
}